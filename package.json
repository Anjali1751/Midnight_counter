{
  "name": "midnight-counter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private-owner counter written in Compact.",
  "scripts": {
    "compile": "compact compile contracts/counter.compact managed",
    "test": "node --test tests/counter.test.ts"
  },
  "engines": {
    "node": ">=22 <23"
  }
}